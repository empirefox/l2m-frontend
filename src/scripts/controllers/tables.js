'use strict';

angular.module('app.navs.tables', ['app.form']).controller('TablesCtrl', ['$scope',
function($scope) {
	//example:
	// $scope.ops = {
	// ban : {
	// recovery : true
	// },
	// fname : $routeParams.fname
	// };
	$scope.ops = {};
}]);
